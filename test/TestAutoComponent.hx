import Doom.*;
import doom.*;
import js.html.*;
import utest.Assert;
import auto.AutoButton;

class TestAutoComponent {
  public function new() {}

  public function testAutoComponent() {
    // normal ctor
    //var autoButton = new AutoButton({ voidApi: voidApi, elementApi: elementApi, eventApi: eventApi }, { intValue: 1, stringValue: "test", enumValue: Secondary });

    // static create ctor
    //var autoButton2 = AutoButton.create(voidApi, elementApi, eventApi, Secondary);
  }
}
